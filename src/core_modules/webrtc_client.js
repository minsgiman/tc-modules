const webrtcClient = {
    init: () => {
        console.log('webrtcClient init');
    },

    streamPlay: () => {
    },

    stop: () => {
    },

    onWebRTCStatusChanged: (status) => {
    }
};

export default webrtcClient;